import React from 'react';
import { Typography, Box } from '@mui/material';

const Branches = () => (
  <Box>
    <Typography variant="h5">Branches (placeholder)</Typography>
    <Typography variant="body2">This is a lightweight placeholder for the Branches admin page.</Typography>
  </Box>
);

export default Branches;
