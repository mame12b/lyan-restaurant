import React from 'react';
import { Box, Typography } from '@mui/material';

const CateringOrders = () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h5">My Catering Orders</Typography>
    <Typography variant="body2">This placeholder shows user catering orders. Implement CRUD and details as needed.</Typography>
  </Box>
);

export default CateringOrders;
