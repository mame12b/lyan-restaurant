import React from 'react';
import { Typography, Box } from '@mui/material';

const RestaurantDetails = () => (
  <Box>
    <Typography variant="h5">Restaurant Details (placeholder)</Typography>
    <Typography variant="body2">Placeholder for restaurant details view.</Typography>
  </Box>
);

export default RestaurantDetails;
