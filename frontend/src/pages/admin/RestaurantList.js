import React from 'react';
import { Typography, Box } from '@mui/material';

const RestaurantList = () => (
  <Box>
    <Typography variant="h5">Restaurant List (placeholder)</Typography>
    <Typography variant="body2">Placeholder for restaurants list. Kept for compatibility.</Typography>
  </Box>
);

export default RestaurantList;
