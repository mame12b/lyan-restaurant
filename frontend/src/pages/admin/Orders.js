import React from 'react';
import { Typography, Box } from '@mui/material';

const Orders = () => (
  <Box>
    <Typography variant="h5">Orders (placeholder)</Typography>
    <Typography variant="body2">Placeholder for admin orders management.</Typography>
  </Box>
);

export default Orders;
