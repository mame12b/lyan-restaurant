import React from 'react';
import { Typography, Box } from '@mui/material';

const Menu = () => (
  <Box>
    <Typography variant="h5">Menu Management (placeholder)</Typography>
    <Typography variant="body2">Placeholder admin menu management page.</Typography>
  </Box>
);

export default Menu;
